@model IEnumerable<AP.Data.Roles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div class="d-flex justify-content-between">
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    @using (Html.BeginForm("FilterByString", "Roles", FormMethod.Get))
    {
        <div class="d-flex pb-2">
            <input class="form-control" type="text" name="value" placeholder="Buscar" />
            <button type="submit" class="btn btn-primary ms-2">Buscar</button>
        </div>
    }
</div>

<table class="table table-responsive table-bordered table-dark table-striped table-primary">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleID })
        </td>
    </tr>
}

</table>
